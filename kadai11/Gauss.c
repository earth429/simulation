#include <stdio.h>
#include <math.h>

#define COLS 10 //行(横)
#define ROWS 10 //列(縦)

// 前進消去
int ForwardElimination(int A[][]) {
    int i, j;

    for (i = 0; i < COLS; i++) {
        for (j = 0; j < ROWS; j++) {

        }
    }
}

int BackwardSubstitution() {
    
}



int main(void) {
    int A[COLS][ROWS];

    int i;

    puts("ガウスの消去法で連立一次方程式を計算しました。");
    return 0;
}